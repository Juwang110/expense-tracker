apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: expense-tracker
    tier: frontend
  ports:
    - protocol: TCP
      port: 80  
      targetPort: 80  
  type: ClusterIP 
